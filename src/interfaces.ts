export interface IPerson {
  name: string;
  avatar: string;
  vote: string;
  pix: boolean;
}

export interface IRank {
  boy: number;
  girl: number;
}

export interface IColors {
  boy: string;
  girl: string;
}
